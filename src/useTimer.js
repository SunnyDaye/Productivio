const useTimer = (totalDuraton) => {
    const isRunning = false;
    const start = () => {};
    const stop = () => {};
    const seconds = totalDuraton; 
    return {isRunning, start, stop, seconds};
}

export default useTimer;